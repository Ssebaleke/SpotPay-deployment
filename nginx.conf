server {
    listen 80;
    server_name spotpay.it.com www.spotpay.it.com;
    client_max_body_size 200M;

    # Redirect HTTP to HTTPS in production
    # Uncomment the following lines after SSL is configured
    # return 301 https://$server_name$request_uri;

    location /static/ {
        alias /app/staticfiles/;
        access_log off;
        expires 30d;
    }

    location /media/ {
        alias /app/media/;
        access_log off;
        expires 7d;
    }

    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }
}

# HTTPS configuration (uncomment after SSL certificate is installed)
# server {
#     listen 443 ssl http2;
#     server_name spotpay.it.com www.spotpay.it.com;
#     client_max_body_size 200M;
#
#     ssl_certificate /etc/letsencrypt/live/spotpay.it.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/spotpay.it.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location /static/ {
#         alias /app/staticfiles/;
#         access_log off;
#         expires 30d;
#     }
#
#     location /media/ {
#         alias /app/media/;
#         access_log off;
#         expires 7d;
#     }
#
#     location / {
#         proxy_pass http://web:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_redirect off;
#     }
# }